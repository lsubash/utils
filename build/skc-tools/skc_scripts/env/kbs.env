SERVER_PORT=9443
SQVS_URL=https://sqvs.server.com:12000/svs/v1
CMS_BASE_URL=https://cms.server.com:8445/cms/v1
AAS_API_URL=https://aas.server.com:8444/aas/v1
KBS_SERVICE_USERNAME=admin@kbs
KBS_SERVICE_PASSWORD=kbsAdminPass
TLS_COMMON_NAME="KBS TLS Certificate"
ENDPOINT_URL="https://kbshostname:9443/v1"
# Key Manager Backend to store keys, KEY_MANAGER can be set to either Directory or KMIP
KEY_MANAGER=KMIP
SKC_CHALLENGE_TYPE="SGX,SW"
CMS_TLS_CERT_SHA384=
TLS_SAN_LIST=<KBS SAN>
BEARER_TOKEN=<KBS Bearer Token>
#Expiry Time in Minutes. Default is 60 minutes
SESSION_EXPIRY_TIME=60
# OPTIONAL - If KEY_MANAGER set to KMIP then need to use following configuration.
KMIP_SERVER_IP=
KMIP_SERVER_PORT=
# Retrieve the following KMIP server’s client certificate, client key and root ca certificate from the KMIP server. 
# This key and certificates will be available in KMIP server, /etc/pykmip is the default path.
KMIP_CLIENT_CERT_PATH=
KMIP_CLIENT_KEY_PATH=
KMIP_ROOT_CERT_PATH=
